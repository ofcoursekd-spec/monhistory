import 'package:flutter/material.dart';

/// Palette MonHistory — unisexe, premium, chaleureuse.
/// Wine + amber gold : évoque le récit, le drame, l'émotion sans le code
/// "rose feminine" du brief initial.
class AppColors {
  static const brand = Color(0xFF7C1D3F);   // burgundy/wine
  static const accent = Color(0xFFD97706);  // amber gold
  static const ink = Color(0xFF1F1F1F);
  static const cream = Color(0xFFFFFBF5);
  static const bg = Colors.white;
}
